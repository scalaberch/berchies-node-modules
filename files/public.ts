const publicFolderName = "public";
const SUBPATH = "/static";

const isPublicFolderEnabled = () => {};

const initiatePublicFolder = () => {
  // ✅ Serve all static files from the "public" folder
  // app.use(express.static(path.join(__dirname, "../public")));
};
